/* eslint-disable @next/next/no-page-custom-font */
/* eslint-disable @next/next/google-font-display */
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import '@/styles/globals.css';
import Image from 'next/image';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
	title: 'YouTube Music',
	description: 'Generated by create next app',
};

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang="ko">
			<head>
				<link
					href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"
					rel="stylesheet"
				/>
			</head>
			<body className={inter.className}>
				{/* navigation bar */}
				<header className="flex flex-row justify-between items-center pl-7 pt-3 pr-40">
					<div className="flex flex-row items-center">
						{/* 이 div width 조절해서 목록 접히고 핀거 구현 예정 */}
						<div className="flex flex-row">
							<div className="mr-7">
								<span className="material-symbols-outlined">menu</span>
							</div>
							<div className="mr-7">
								<Image
									src={'/logo.png'}
									alt=""
									width={100}
									height={100}
								/>
							</div>
						</div>

						<div className="w-max flex items-center rounded-lg bg-[#FFFFFF26] border-[1px] border-[#FFFFFF80] p-3">
							<span className="material-symbols-outlined mr-3">search</span>
							<input
								className="bg-transparent border-none outline-none w-96"
								type="search"
								placeholder="노래, 앨범, 아티스트, 팟캐스트 검색"
							/>
						</div>
					</div>

					<div className="flex flex-row">asasd</div>
				</header>
				<aside className="w-48">
					<div className="flex flex-col items-center pt-5 w-20">
						{new Array(3).fill(null).map((o, i) => {
							const icon = 'home,explore,library_music'.split(',')[i];
							const text = '홈,둘러보기,보관함'.split(',')[i];
							const closeMenu = (
								<button
									key={i}
									className="mb-5"
								>
									<span className="material-symbols-outlined">{icon}</span>
									<p className="text-xs">{text}</p>
								</button>
							);

							return closeMenu;
						})}
					</div>
				</aside>
				{children}
			</body>
		</html>
	);
}
